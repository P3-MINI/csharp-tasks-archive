

//Stage 1: (2.5pkt)
//-Implement `CircullarBuffer<T> : IBuffer < T >`
//-Constructor with max-items
//- `Put` method - throw with `Full buffer`
//- `Get` method, oldest, throw with `Empty buffer`
//- `Size` property - max items
//- `Count` property - current number of items
//- Properties `Empty`+`Full`
//- `Reset` method - clean buffer

//Stage 2: (1.5pkt)
//- `IEnumerable` in `LIFO` order
//- `FilterLowerThan` method

//Stage 3: (1pkt)
//- `Clone` method